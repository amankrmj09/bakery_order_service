server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: bakery-order-service
  profiles:
    active: ${ACTIVE_PROFILE:dev}
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8081}"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
