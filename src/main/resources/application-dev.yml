spring:
  datasource:
    url: jdbc:postgresql://blubug.me:8003/bakery_orders
    username: order_user
    password: order_password
  kafka:
    bootstrap-servers: localhost:9092
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
